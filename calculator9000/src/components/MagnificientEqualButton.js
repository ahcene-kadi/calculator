import React from 'react'

const MagnificientEqualButton = (props) => {
  return (
    <>
      <button className="calc-button egale" onClick={props.rend}>=</button>
    </>
  );
}

export default MagnificientEqualButton;